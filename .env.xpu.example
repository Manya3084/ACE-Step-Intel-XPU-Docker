# =============================================================================
# ACE-Step 1.5 — Sample .env optimized for Intel Arc A770 16GB
# =============================================================================
#
# Usage:
#   cp .env.xpu.example .env
#
# Default is API mode so you can use ace-step-ui (or any other frontend)
# instead of Gradio.
# =============================================================================

# ==================== Mode ====================
# api = REST API only (recommended, use with ace-step-ui)
# gradio = old Gradio web UI
ACESTEP_MODE=api

# ==================== Model Settings ====================
ACESTEP_CONFIG_PATH=acestep-v15-turbo
ACESTEP_LM_MODEL_PATH=acestep-5Hz-lm-1.7B
ACESTEP_DEVICE=xpu
ACESTEP_LM_BACKEND=pt

# ==================== LLM Initialization ====================
ACESTEP_INIT_LLM=true
ACESTEP_OFFLOAD_TO_CPU=true

# ==================== API Server ====================
ACESTEP_API_HOST=0.0.0.0
ACESTEP_API_PORT=8001

# ==================== Performance / Stability ====================
PYTORCH_DEVICE=xpu
TORCH_COMPILE_BACKEND=eager
SYCL_CACHE_PERSISTENT=1
SYCL_PI_LEVEL_ZERO_USE_IMMEDIATE_COMMANDLISTS=1
ZE_FLAT_DEVICE_HIERARCHY=COMPOSITE
TOKENIZERS_PARALLELISM=false
ACESTEP_BATCH_SIZE=1

# ==================== Optional ====================
# ACESTEP_API_KEY=sk-change-me
